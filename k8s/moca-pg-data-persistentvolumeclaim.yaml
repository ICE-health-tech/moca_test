apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: moca-pg-data
  name: moca-pg-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
